# Redis operator install.sh env vars
# Copy and edit, then: source .env && ./install.sh

# Deploy mode: standalone | replication | cluster  (default: standalone)
REDIS_MODE=standalone

# If set, creates a redis-secret in the redis namespace.
# After setting, uncomment redisSecret in the matching CR YAML.
# REDIS_PASSWORD=
