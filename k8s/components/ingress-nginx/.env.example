# nginx-ingress-controller component — environment variables
# Copy to .env and fill in values for your cluster.
# No variables are required by the current __values.yml template.
# Add entries here when you introduce ${VAR} placeholders in __values.yml.
