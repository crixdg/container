# CloudNativePG Operator — active values

replicaCount: 1

resources:
  requests:
    cpu: 100m
    memory: 200Mi
  limits:
    cpu: 500m
    memory: 500Mi

monitoring:
  podMonitorEnabled: false
  grafanaDashboard:
    create: false
