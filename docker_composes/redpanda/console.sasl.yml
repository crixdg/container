services:
  console:
    environment:
      KAFKA_BROKERS: "redpanda:9093"
      KAFKA_SASL_ENABLED: "true"
      KAFKA_SASL_MECHANISM: SCRAM-SHA-256
      KAFKA_SASL_USERNAME: "${REDPANDA_SASL_USER}"
      KAFKA_SASL_PASSWORD: "${REDPANDA_SASL_PASSWORD}"
