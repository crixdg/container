# k3s server configuration — used by scripts/install-server.sh only.
# Copy to scripts/.env and fill in your values before running the script.
#
# For Ansible provisioning, set variables in ansible/group_vars/all.yml instead.

# ------- Node identity --------------------------------------------------------
# IP of THIS server node — used as --node-ip and written into the API TLS cert.
NODE_IP=192.168.1.2

# Version pin — always pin in production.
# Find releases at: https://github.com/k3s-io/k3s/releases
K3S_VERSION=v1.35.4+k3s1

# ------- Networking -----------------------------------------------------------
CLUSTER_CIDR=10.42.0.0/16
SERVICE_CIDR=10.43.0.0/16

# Extra IPs or hostnames added to the API server TLS cert (SAN).
# Add your load-balancer VIP or DNS name so remote kubectl works.
# Comma-separated. Example: "lb.example.com,10.0.0.10"
EXTRA_SANS=""

# ------- Storage --------------------------------------------------------------
DATA_DIR=/var/lib/rancher/k3s

# Set to true only after Longhorn is installed and PVCs are migrated.
DISABLE_LOCAL_STORAGE=false

# ------- CNI / kube-proxy -----------------------------------------------------
# Disable kube-proxy when using Cilium in kube-proxy replacement mode.
DISABLE_KUBE_PROXY=true

# Disable flannel when using a third-party CNI (Cilium, Calico).
DISABLE_FLANNEL=true

# ------- Built-in components --------------------------------------------------
# Disable components replaced by external equivalents.
DISABLE_TRAEFIK=true     # using ingress-nginx instead
DISABLE_SERVICELB=true   # using MetalLB or cloud LB instead
